'use client';

/**
 * BottomNav has been deprecated in favor of the Sidebar Menu in the Header for mobile devices.
 * Returning null to prevent rendering.
 */
export default function BottomNav() {
  return null;
}
